import { makeStyles } from "@material-ui/core";

export const ProjectFormStyles = makeStyles((theme) => ({
    

    projectInput:{
        marginBottom:'1em'
    },

    projectTitle:{

        textAlign:'center'
    }

}))